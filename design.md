# 若水广场 Design

## 1. Visual Theme

- 关键词：纪念感、夜色、航拍、安静、克制
- 整体气质不是科技面板，而是“场景上覆一层很轻的展陈壳”
- 手机端要比桌面端更明确地做信息收敛，先给方向感，再给控制项

## 2. Color And Surface

- 背景基调保持深夜蓝与树皮棕的混合，不要改成纯黑或冷白
- 面板用半透明暖棕玻璃，不要加入蓝色环境反光
- 强调色继续使用嫩叶绿，只用于状态、当前选中、可操作入口
- 手机端浮层需要比桌面端更实一点，避免 outdoor 光线下完全糊成一片

## 3. Typography

- 标题保留紧凑、略带纪念感的压缩感字距
- 手机端正文宁可更短，也不要缩成难读的小字
- 手机端辅助信息统一使用短句和标签式表达，不堆技术说明

## 4. Layout Principles

- 场景永远是第一视觉层
- 手机端首屏只保留三块内容：
  - 左上纪念信息与当前状态
  - 右上小地图
  - 底部单手可触达控制坞
- 不要让手机端出现“桌面侧栏缩进来”的感觉

## 5. Mobile Interaction

- 手机端优先支持“先看、再导览、再细调”的路径
- 最常用的两个入口固定在底部：模型版本、导览镜头
- 控制面板使用底部抽屉，但抽屉顶部要有当前上下文、快捷切换和收起动作
- 所有手机端主操作控件触控高度至少 `44px`
- 适配 `safe-area-inset-*`，底部控件和抽屉都不能贴死边缘

## 6. Component Rules

- 小地图在手机端是“方向感锚点”，尺寸可以更小，但必须有简短标题
- 底部控制坞要像一个完整组件，而不是一个漂浮按钮
- 状态条、标签、chip 可以更像策展导视系统，不要做成后台工具栏
- 抽屉内部保持一层信息摘要，然后再进入折叠控制区

## 7. Motion

- 抽屉只做短距离上滑和淡入，不要弹跳
- 按钮选中态优先用颜色与边框，不要做夸张位移
- 动效节奏短、稳、安静

## 8. Don’ts

- 不要把手机端做成缩小版桌面工具栏
- 不要在手机端同时展示过多技术指标
- 不要让浮层遮住场景的核心阅读区域
- 不要引入亮紫、霓虹蓝这类偏赛博的高饱和强调色
